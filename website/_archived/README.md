# Archived site pages

Kept in the repo for reference but not served by the Next.js app.

- `install/` — former `/install` (Get started)
- `guides/` — former `/guides` and guide articles
- `InstallPicker.tsx`, `AgentIcons.tsx`, `install-clients.ts` — only used by Get started
